Schema version: {schema_version}

Response schema JSON:
{response_schema_json}

The previous response should have been a single JSON object matching the schema.
Repair syntax only.
Keep facts unchanged.
Remove invalid items only when they cannot be repaired safely.

Invalid response:
<<<INVALID_JSON
{invalid_json}
INVALID_JSON
